// app/(tabs)/settings.tsx — Settings tab route
// AUDIT FIX: Previously navigated to Scanner — now has a real Settings screen
import SettingsScreen from '@/screens/SettingsScreen';
export default SettingsScreen;
